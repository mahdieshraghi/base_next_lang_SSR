import type { ThemeConfig } from "@/theme-engine/types";

export const config: ThemeConfig = {
  name: "minimal",
  label: "Minimal",
  description: "Clean minimal design with sidebar layout",
  version: "1.0.0",
  author: "System",
};

